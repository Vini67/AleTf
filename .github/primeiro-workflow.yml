jobs:
  primeiro-job:
    runs-on: ubuntu-latest

    # Aqui criamos variáveis de ambiente para todo o job
    env:
      REPO_NAME: ${{ github.repository }}
      BRANCH_ATUAL: ${{ github.ref }}
      USUARIO: ${{ github.actor }}

    steps:
      - name: Imprime uma mensagem de boas-vindas
        run: echo "👋 Olá, Mundo! Meu primeiro workflow com GitHub Actions está funcionando, é isso ai!"

      - name: Mostra o diretório de trabalho
        run: pwd

      - name: Lista os arquivos no runner
        run: ls ~ -la

      - name: Mostra a variável do repositório
        run: echo "O nome do repositório é $REPO_NAME"

      - name: Mostra a branch atual
        run: echo "A branch atual é $BRANCH_ATUAL"

      - name: Mostra o usuário que disparou o workflow
        run: echo "O workflow foi executado por $USUARIO"
